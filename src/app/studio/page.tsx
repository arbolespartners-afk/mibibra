import { HouseStudio } from '@/components/studio/HouseStudio';

export const metadata = {
  title: 'House Studio — mibibra',
  description: 'Crea música house con el secuenciador de mibibra.',
};

export default function StudioPage() {
  return <HouseStudio />;
}
